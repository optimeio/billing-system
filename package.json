{
  "name": "billing-system",
  "version": "1.0.0",
  "description": "Premium Billing Software System",
  "scripts": {
    "mail-prompt": "node backend/utils/mailPrompt.js",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -k -s first \"npm run backend\" \"npm run frontend\"",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "mongoose": "^9.6.2",
    "node-fetch": "^3.3.2",
    "nodemailer": "^8.0.10",
    "nodemon": "^3.1.14",
    "vite": "^8.0.16"
  }
}
